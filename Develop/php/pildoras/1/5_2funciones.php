<?php
echo "parametros por referencia y por valor";

//por default las funciones pasan parametros por valor
//funcion($param)...

/*recordeos que el paso de objetos por valor, usan y modifican el contenido aa nivel 
de los procedimientos, funciones, etc; que lo utilizan, pero el valor en la posicion 
de la memoria no cambia. Por referencia es lo contrario, cambiara el valor en la 
posicion de la memoria*/

//para pasar por referencia se utiliza
//funcion(&$param)...

?>